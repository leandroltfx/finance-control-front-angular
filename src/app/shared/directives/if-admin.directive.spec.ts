import { IfAdminDirective } from './if-admin.directive';

describe('IfAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAdminDirective();
    expect(directive).toBeTruthy();
  });
});
